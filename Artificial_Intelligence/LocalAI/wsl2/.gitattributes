# Shell scripts MUST stay LF. core.autocrlf=true would otherwise check them
# out as CRLF on Windows, which breaks WSL shebangs with
# "/usr/bin/env: 'bash\r': No such file or directory".
*.sh text eol=lf
